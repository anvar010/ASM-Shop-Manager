# Copy to .env.local and fill in. Never commit the filled-in file.
# On Vercel these go under Settings → Environment Variables.

DB_HOST=
DB_PORT=3306
DB_NAME=asm
DB_USER=asm_user
DB_PASSWORD=
DB_SSL=false

# Generate with: openssl rand -base64 48
AUTH_SECRET=
